import styles from './Footer.module.scss';

const Footer: React.FC = () => {
  return (
    <footer className={styles.footer}>
      <p>Paweł Skórka, Moodlyzer v1.0</p>
    </footer>
  );
};

export default Footer;
